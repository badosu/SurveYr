<% flash.each do |key, msg| %>
    <%= content_tag :div, msg, :class => [key, " message"], :id => "flash_#{key}" %>
    <%= content_tag :script, :type => "text/javascript" do -%>
      setTimeout(function(){
        $('#flash_<%= key %>').fadeOut('slow');
      }, 5000);
    <% end %>
<% end %>
